services:
  postgres:
    image: postgres:16-alpine
    container_name: manzil-e2e-pg
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_DB: manzil_e2e_db
    ports:
      - '5433:5432'
